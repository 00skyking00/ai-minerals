"""Feature builders for the prospectivity grid.

Each module here produces one or more columns of features aligned to the
`Grid` defined in `ai_minerals.grid`. Everything works on the centroid
GeoDataFrame from `grid.centroid_gdf()`.
"""
