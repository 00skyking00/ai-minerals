# data/raw/

Raw downloads from USGS, Geoscience Australia, Microsoft Planetary Computer, and similar public sources. Gitignored by default — see fetch scripts (Day 2+) for how to populate.

Each dataset goes in a named subdirectory with a short `SOURCE.md` documenting the URL, retrieval date, and license.
